[build]
  command = "npm run build"
  publish = "dist"

# Edge Function 설정
[[edge_functions]]
  path = "/customer"
  function = "og-proxy"

# SPA 라우팅 - 모든 경로를 index.html로
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
